# realsense d405 sdk
pyrealsense2>=2.55.0

# point cloud and mesh processing
open3d>=0.18.0
numpy>=1.24.0

# toolpath generation
# opencamlib: build from source on arm64/trixie
# see README for instructions

# g-code streaming
pyserial>=3.5

# config loading
pyyaml>=6.0

# clearcore modbus communication
pymodbus>=3.6.0

# ui dependencies (separate venv if python version conflicts)
PyQt6>=6.5.0
pyvista>=0.43.0
pyvistaqt>=0.11.0
